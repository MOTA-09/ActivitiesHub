info:
  name: Events - List - 200
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
